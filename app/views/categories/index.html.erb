
<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/breakfast.jpg)">
<h1>View your cookbook <strong>categories</strong> here</h1>
</div>




<h1>View your categories</h1>

    <ul>
      <% @categories.each do |category| %>
      <li><%= link_to category.name.capitalize, category_path(category) %> 

      | <%= link_to "delete", category_path(category), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
      <% end %>
    </ul>

    <%= link_to "add a new category", new_category_path %>
